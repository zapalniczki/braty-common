{
  "name": "braty-common",
  "version": "1.0.4",
  "main": "dist/index.js",
  "license": "UNLICENSED",
  "repository": "https://github.com/zapalniczki/braty-common",
  "private": true,
  "devDependencies": {
    "husky": "^7.0.4",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "build": "tsc",
    "minor": "yarn version --new-version minor",
    "patch": "yarn version --new-version patch"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn run patch"
    }
  }
}
